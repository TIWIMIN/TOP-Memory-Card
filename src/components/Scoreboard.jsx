import { useState } from "react";

export default function Scoreboard({pokeIndexes}) {
  
    [score, setScore] = useState(0); 
    [bestScore, setBestScore] = useState(0); 
    []

  return (
    <>
      <div>hi</div>
    </>
  );
}
